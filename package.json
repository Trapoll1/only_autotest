{
  "name": "only-digital-footer-tests",
  "version": "1.0.0",
  "description": "Automated tests for Only Digital website footer",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "dependencies": {
    "@playwright/test": "^1.42.1",
    "playwright": "^1.42.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.24"
  },
  "engines": {
    "node": ">=14"
  }
}
